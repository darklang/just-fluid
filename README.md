# Just-Fluid

This is a fork of the adjacent `classic-dark` repo.

It's an experiment trimming down a bunch of stuff,
leaving _just_ the Fluid bits,
which were the bits that handled structured editing.

I'm not sure if we'll ever do anything with it,
but might as well retain this fork somewhere.